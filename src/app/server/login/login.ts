import {UsersService} from '../../services/users/users.service';

module loggin {
    export function log(currentUser: any, users: any) {
    if ( currentUser.mail === users.mail && currentUser.password === users.password ) {
      
    }
    }
}